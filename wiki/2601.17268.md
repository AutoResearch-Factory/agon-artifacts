# Learning Accurate Storm-Scale Evolution from Observations (Stormscope) (2601.17268)

- **Year**: 2026.01
- **Authors**: Jaideep Pathak（StormCast 一作/本文一作）, Mohammad Shoaib Abbas, Peter Harrington, Zeyuan Hu, Noah Brenowitz, Suman Ravuri, Alberto Carpentieri, Jussi Leinonen, Corey Adams, Oliver Hennigh, Nicholas Geneva, Dale Durran, Mike Pritchard — 全员 NVIDIA Corporation（不同于 StormCast 尚有 LBNL/UW/UMN 学术合作者，本文全部作者单一 NVIDIA 归属）
- **Venue**: arXiv preprint（正文未见期刊/会议接收声明）
- **Keywords**: diffusion transformer (DiT), storm-scale nowcasting/nearcasting, GOES-16 satellite imagery, MRMS radar, observation-direct forecasting（非NWP场初始化）, 2D Neighborhood Attention, EDM diffusion, domain parallelism, ensemble forecasting
- **One-liner**: NVIDIA StormCast 一作团队的后续工作，用纯 Diffusion Transformer 直接在 GOES 卫星+MRMS 雷达观测（非 NWP 场）上训练，6km/10min 分辨率下 1-5h 提前量优于/持平 HRRR。

## 精华提炼

### 管线/方法

Stormscope 是一个单阶段（非 StormCast 的"回归+扩散"两阶段）Diffusion Transformer (DiT) 架构，提供两类模型：

1. **Nowcasting（0-2h，10min 分辨率）**：纯观测驱动，不吃任何 NWP 场。输入为过去 1 小时内每 10 分钟一帧、共 **6 帧** 的 GOES 卫星（8 通道）与 MRMS 雷达（1 通道，反射率）历史堆栈，通道拼接（channel-concat）后送入模型，预测下一个 10 分钟状态。
2. **Nearcasting（0-12h，1h 分辨率）**：额外引入稀疏 NWP 条件——卫星分支只吃 **前一小时单帧** GOES+MRMS（不是堆栈）外加 ERA5（训练）/GFS（推理）的 500hPa 位势高度（Z500，单通道、单层）；雷达分支额外吃 GFS 模拟合成反射率（单通道）。
3. **DiT 主干**：输入网格 512×896（6km，源自 HRRR 网格降采样），先经步幅卷积 patch embedding（patch size 4×4）转为 128×224 个 token、embedding dim 768，再过 16 层 transformer block，用标准 DiT 条件层归一化（adaLN）注入扩散时间步。为降低全局 attention 的计算成本，用 2D Neighborhood Attention（NA，局部窗口 31×31）替代全局 attention。
4. **训练目标**：EDM（Karras et al. 2022）框架，$L(\theta)=\mathbb{E}_{x,\sigma,n}[\lambda(\sigma)\lVert \mathcal{D}_\theta(x_{t+}+n_\sigma;x_{t-};c;\sigma)-x_{t+}\rVert_2^2]$；采用 multi-expert 策略把噪声区间切成粗/细两个 regime 各训一个 expert（沿用 eDiff-I / Brenowitz "Climate in a Bottle" 的思路）。
5. **自回归 rollout**：预测出的 $\tilde x(t)$、$\tilde y(t)$ 滑窗喂回作为下一步输入，卫星与雷达模型各自独立训练/采样（雷达分支以卫星分支的预测为条件，二者非联合训练）。
6. **推理**：EDM 二阶 ODE 采样器（Karras et al.），100 步，fp32 全精度单步约 33 秒（单 H100），12h nearcast 全程约 7 分钟。

### 关键技术细节

- **状态变量表（Table 1，共 12 行，与正文"8 个卫星通道"表述完全一致，无 StormCast 式表格-正文不一致问题）**：8 个 GOES-16 ABI 通道（Blue 0.47μm, Red 0.64μm, Veggie 0.86μm, Shortwave Window 3.90μm, Upper/Mid/Lower Level Water Vapor 6.19/6.95/7.34μm, Clean IR Longwave Window 10.35μm）+ MRMS 雷达合成反射率（1 通道）+ Z500（1 通道，仅 nearcast）+ GFS 模拟合成反射率（1 通道，仅 nearcast）+ 太阳天顶角余弦（计算得到，1 通道）。**全文没有任何气压层×多变量的大气状态张量**——唯一带"气压层"字样的是 Z500 这一个标量场。
- **训练数据**：GOES 数据 2018-2023，MRMS 数据 2020-2023；卫星原生数据先重映射到 HRRR 用的 3km Lambert Conformal Conic 网格，再降采样到 6km、10 分钟节奏。评测用 2024 年全年 360 个随机采样初始化时刻（与 StormCast 的 2024 春季窗口、HRRRCast 的 2024 年 4-7 月/5 月窗口属同一年份，可横向对比）。
- **模型规模与算力**：195M 参数，训练显存约 70GB；每个（卫星或雷达）模型在 32×H100 上训练约 48 小时、约 600 万迭代，batch size 32，Stable AdamW，LR 5e-4→0 余弦衰减。对比 StormCast 扩散阶段单独就用了 64×H100×120 小时——Stormscope 报告的总训练算力明显更低（但架构/目标函数不同，非严格可比）。
- **3km/CONUS 全域扩展（domain parallelism，Section 3.2）**：用 NVIDIA PhysicsNeMo 的 `ShardTensor` API 做 2D 空间域并行（domain-parallel size=2, data-parallel size=4），训练了一个 3km、CONUS 全域（1792×1024 网格，约 180 万像素）的**纯卫星 nowcast 模型**，达到 87.5% 的并行效率。**关键限定**：这个 3km 模型只是并行框架的工程可行性演示，论文全篇没有为它报告任何 FSS/CRPS/reliability 等技能评分——所有第 2-3 节的定量结果（FSS、CRPS、object-based 指标）都只针对 6km 主模型，3km 模型的实际预报技能未知。
- **模型自身对 StormCast/HRRRCast 的明确定位（原文原话，非我的转述）**：Introduction 第 4 段——"Prior work on AI emulators of convection-allowing models [StormCast, HRRRCast, WoFSCast, Nipen et al.]...rely on the data-assimilation infrastructure of their physics-based counterparts to initialize forecasts and for training data, thus inheriting the limitations of physics-based state estimates."——Stormscope 把 StormCast/HRRRCast 归为同一类"仿真器"（emulator），并明确以此为对照提出自己的范式差异：不依赖任何 DA 基础设施，直接从观测学习。

### 核心结果

| 指标 | 发现 |
|---|---|
| FSS 反射率 20/30dBZ（nearcast） | Stormscope 单成员确定性预报在 3h 内优于 HRRR；5h 内维持competitive |
| FSS 红外 235K/225K 阈值（nearcast） | 与雷达一致，3h 内优于 HRRR 的模拟亮温预报 |
| CRPS（概率，nearcast） | 5 成员 Stormscope 集合优于 5 成员滞后集合（lagged-ensemble）HRRR |
| Reliability diagram | 20/30dBZ 阈值下曲线均在对角线以下——模型系统性过度自信（overconfident），1/3/6h 提前量一致 |
| FSS（nowcast, 40dBZ, vs pySTEPS） | 6km 尺度上两者技巧相近且偏低；随池化尺度增大 Stormscope 明显优于 pySTEPS，20 分钟后差距扩大 |
| Object-based（POD/SR/CSI/FB, vs pySTEPS） | Stormscope 的 FB 更接近 1、CSI/POD 系统性更高；SR 未一致改善 |
| 统计显著性 | 配对 t 检验 + bootstrap 95% CI：FSS@20dBZ 全部提前量显著（p<0.05）；FSS@30dBZ 除 4h 外全部显著 |
| GFS 反射率条件消融 | 加 GFS 模拟反射率条件 > 不加，且提前量越长差距越大 |

**Surprising**：论文明确承认"未与最相关的两个雷达-only nowcasting 强基线（NowcastNet, Ravuri et al. DGMR）直接比较，因此不宣称在 0-2h 雷达-only nowcasting 上是 SOTA"——罕见的克制表态，同时也说明其自选基线（pySTEPS、HRRR）避开了当前该子任务最强的两个 ML 竞争对手。

### 明确局限

**作者自述**：nearcast 条件极稀疏（仅 Z500 一个通道，作者承认更密的条件可能带来额外增益，并做了 Supplementary 消融验证"有 GFS 反射率条件 > 无"）；未处理陡峭视角下的 parallax 误差；未与 NowcastNet/DGMR 直接比较、不宣称 0-2h 雷达 nowcasting SOTA；6km 分辨率不如 pySTEPS 可用的原生 1-2km；HRRR 滞后集合只是物理概率基线的权宜之计，非真正的物理集合（如 WoFS 尺度）；集合欠离散/过度自信（reliability diagram 已量化展示）。

**我读出的、与本任务直接相关的局限**：
- **全文（含 Table 1 和 Methods）不存在任何气压层×多变量的大气状态张量**——除 Z500 外没有第二个大气层次的场，这是和 causal-scs-indicator 拟定的"4 气压层×lon×lat×time"输入契约在**表示范式**层面的根本不兼容，比 StormCast/HRRRCast 的"气压层定义不完全匹配"更严重——后两者至少有真实的多层大气状态变量可以尝试对齐/插值，Stormscope 里根本没有可对齐的对象。
- **历史窗口远小于 96h**：nowcast 6 帧×10min=1 小时，nearcast 仅 1 帧（T-1h）——比 nowcast 更短。两者都比目标 96h 小两个数量级以上，且历史编码是简单的"堆帧通道拼接后一起过 2D patch embedding"，不是独立的时序编码轴（没有专门的时间维 attention/RNN/temporal-conv 模块）——不存在"冻结时序编码器"这个选项，因为压根没有一个可拆卸的时序子模块。
- **3km 结果没有技能评分**：论文所有定量验证都在 6km 网格上完成；被反复引用作为"扩展到 3km 能力"的 domain-parallel 章节只报告了并行效率数字（87.5%），不能作为"3km 下也能达到 6km 同等技巧"的证据。
- **全文（含 Acknowledgments）未见任何代码/权重开源声明**——用 `grep -niE "github|zenodo|huggingface|checkpoint|code availab|data availab"` 核对全文零命中；Acknowledgments 感谢的是"NVIDIA PhysicsNeMo team"提供的工具（PhysicsNeMo 本身是 NVIDIA 开源训练框架，但这不等于本文训练出的模型权重公开）——与 StormCast 相同的"沿用团队内部工具、不发布本文权重"模式。
- 训练卫星源仅 GOES-16（GOES-East，覆盖美洲），论文自己在 Conclusion 展望"extend...to oceanic regions and countries"依赖其他geostationary卫星（Himawari/Meteosat），但本文未做任何跨卫星验证，可推广性目前只是论述而非实证。

### R-Score（Reliability score / 科学可信度）

| R1 Evidence | R2 Independ | R3 Honesty | R4 Reprod | R5 Subst | Tier |
|-------------|-------------|------------|-----------|----------|------|
| 4 | 3 | 5 | 2 | 5 | A (19) |

- **R1 Evidence Grounding**：FSS/CRPS/reliability/object-based 四类指标均有具体数字、阈值、池化尺度、显著性检验（配对 t 检验+bootstrap CI）支撑，可追溯性强；Table 1 与正文"8 通道"表述一致，未发现 StormCast 式的表格-正文数字矛盾。定性结论（如"ensemble 展示令人满意的内部多样性"）仍主要靠个案可视化判断。
- **R2 Verification Independence**：基线含独立的物理模式（HRRR）和独立观测产品（MRMS/GOES 本身），但同时明确回避了与该子领域最强的两个独立 ML 基线（NowcastNet、DGMR）直接对比——是诚实披露的选择性验证缺口，而非隐藏；故不给到 StormCast 的 4 分。
- **R3 Honesty**：主动披露稀疏条件、parallax 未处理、未对比 NowcastNet/DGMR 因而不宣称 SOTA、6km 分辨率劣势、集合过度自信（reliability diagram 直接量化展示而非文字带过）——诚实度维持高位。
- **R4 Reproducibility**：训练数据源（GOES/MRMS/GFS/ERA5）公开，核心超参数（patch size、层数、embedding dim、NA 窗口、EDM 噪声调度、优化器、学习率、batch、迭代数、GPU-小时）给得比较具体；但零代码/权重发布声明，domain-parallel 3km 训练的具体工程细节（数据管线、shard 策略之外的实现）不足以直接复现；较 StormCast 的"至少有超参数交代较完整"打了折扣，给 2 分。
- **R5 Substantiation**：NVIDIA 具名团队（多位与 StormCast 重合），真实案例研究（具体日期地点：2024-06-25 MCS、2024-03-14 堪萨斯风暴、2024-05-15 发展中 MCS)，工程细节具体到位（87.5% 并行效率、GPU 拓扑图），且论文自己主动与 StormCast/HRRRCast 做范式对比、诚实划定与 NowcastNet/DGMR 的比较边界——外部可核查、扎实。

### 跟我们的关系

对 causal-scs-indicator：Stormscope **不适合**作为 P2 raw-only 分支的预训练权重来源——原因不是"细节不匹配需要改造"（StormCast/HRRRCast 的情况），而是**范式层面根本没有可复用的对象**：Stormscope 的核心状态空间是卫星辐射率+雷达反射率图像，全文只有一个气压层变量（Z500，单通道稀疏条件），不存在"4 气压层×lon×lat"这样的多层大气张量可以拆出来对齐或插值。它对本任务唯一有参考价值的是一个**已验证的架构设计模式**（把历史帧沿通道维拼接后送入 2D patch embedding + Neighborhood Attention 的 DiT，被证明能在真实数据上跑赢 HRRR），可以作为团队自己动手做"96h/4 气压层/3km"自监督预训练时的设计参考，而不是权重复用来源。详细对比与最终排序见下方 Read by 章节。

## 引文（本文引用的相关论文）

| arxiv_id | 标题 | 年份 | 为什么相关 |
|----------|------|------|-----------|
| 2408.10958 | Kilometer-scale convection allowing model emulation using generative diffusion modeling (StormCast) | 2024 | 直接前作/同一作者组，本文 Introduction 明确将其归为"依赖物理 DA 基础设施的仿真器"类并与自己范式对比 |
| 2507.05658 | HRRRCast: a data-driven emulator for regional weather forecasting at convection allowing scales | 2025 | 与 StormCast 并列，本文同样归为"仿真器"类对照对象 |
| — (DOI:10.1029/2024GL112383) | WoFSCast: A Machine Learning Model for Predicting Thunderstorms at Watch-to-Warning Scales (Flora & Potvin) | 2025 | Stormscope 的 object-based 验证方法（40dBZ 阈值、面积/位移匹配参数）直接沿用 WoFSCast 先例 |
| 2505.06474 | Climate in a Bottle: Towards a Generative Foundation Model for the Kilometer-Scale Global Atmosphere (Brenowitz et al.) | 2025 | 同一 NVIDIA 团队的生成式基座模型，Stormscope 的 multi-expert 噪声切分策略直接沿用该文 |
| 2504.16922 | Generalized Neighborhood Attention: Multi-dimensional Sparse Attention at the Speed of Light (Hassani et al.) | 2025 | Stormscope DiT 主干核心机制（2D Neighborhood Attention）的直接方法来源 |
| 2409.02891 | Regional data-driven weather modeling with a global stretched-grid (Nipen et al.) | 2024 | 与 StormCast/HRRRCast 并列，同属 Introduction 点名的"AI 仿真器"类前作 |
| 2401.15305 | A Practical Probabilistic Benchmark for AI Weather Models (Brenowitz et al.) | 2024 | 同组概率评测方法论参考 |
| 2510.13050 | An Operational Deep Learning System for Satellite-Based High-Resolution Global Nowcasting (Agrawal et al.) | 2025 | 本文唯一详细对比讨论的"多模态观测融合"直接竞争范式（Google），原文明确区分二者目标（分布预测 vs 动力学轨迹生成） |
| 2306.03838 | Spherical Fourier Neural Operators: Learning Stable Dynamics on the Sphere (Bonev et al.) | 2023 | 全球尺度 ML 天气预测先例，Stormscope 引用作为"ML 已在粗分辨率证明可行"的背景 |
| 2412.15832 | AIFS-CRPS: ensemble forecasting using a model trained with a loss function based on CRPS (Lang et al.) | 2024 | CRPS 损失/评测先例 |

## 反引文（引用了本文的后续工作）

2026-07-14（0713-scs-benchmark 轮）S2 重试成功，共 4 条，与上一轮被限流前瞥见的"Total citations: 4"汇总数一致（上一轮三次重试均 HTTP 429 未取得条目）：

| arxiv_id | 标题 | 年份 | 为什么相关 |
|----------|------|------|-----------|
| 2607.07879 | Global reanalysis from observations alone with machine learning (Lean, Pinnington, Laloyaux et al., ECMWF) | 2026 | 观测直驱范式向全球再分析端扩张的独立后续（非 NVIDIA 团队），佐证 obs-direct 趋势线 |
| 2605.11111 | ShardTensor: Domain Parallelism for Scientific Machine Learning (Adams, Harrington et al.) | 2026 | 同 NVIDIA 团队把本文所用 domain-parallel 训练机制单独成文，工程向 |
| 2605.03802 | Towards accurate extreme event likelihoods from diffusion model climate emulators (Manshausen, Brenowitz et al.) | 2026 | 同团队后续：扩散模型的极端事件似然评估，与事件级评测视角相关 |
| 2604.00689 | Performance of Neural and Polynomial Operator Surrogates (Westermann et al.) | 2026 | 引本文作为神经代理模型应用实例，与本课题相关性低 |

## Read by: 0710-causal-scs (2026-07-13)

**任务定位**：proposal v1（REVISE 判决）Frontier Leverage 弱项的第二轮定向调研——第一轮已分别精读 StormCast（2408.10958）与 HRRRCast（2507.05658）并发现两者共同的致命缺陷是"单时刻输入、完全没有历史维度"；HRRRCast wiki 明确把本文（2601.17268，StormCast 同一作者组 2026 年最新后续工作）列为下一轮必须核实的候选，猜测其"可能是原生 3km 的后继模型"。本轮精读结论：**这个猜测部分成立（架构确有进步）但结论对本任务是否定的**——原因和第一轮预期的方向不同，细节如下。

### 四维度对比表（StormCast vs HRRRCast vs Stormscope）

| 维度 | StormCast (2408.10958) | HRRRCast (2507.05658) | Stormscope (2601.17268) |
|---|---|---|---|
| **时序历史编码** | 无——单时刻输入 $M_t,S_t$ → 单时刻输出 $M_{t+1}$，自回归仅靠把输出喂回做下一步输入 | 无——单时刻状态 $t$ + GFS 强迫 $t+L$ → 单时刻状态 $t+L$，Figure 1 无任何时序轴 | **有但远不足 96h**：nowcast=6 帧/1h（10min 间隔），nearcast=1 帧/T-1h（比 nowcast 更短）；机制是堆帧通道拼接后统一过 2D patch embedding，**不是独立时序编码轴**，无可拆卸的"历史编码器"模块 |
| **气压层覆盖** | $M_t$：16 层原生混合坐标（非气压层）；$S_t$：4 个标准气压层（1000/850/500/250hPa）但为 ERA5/GFS 粗分辨率插值，通道拼接非独立编码器 | HRRR 侧 12 个真实气压层（200-1000hPa，含 850/1000 两个与目标精确重合，500/250 无精确对应，最近为 475/300或200hPa）；GFS 侧 4 层 | **实质为零**：全文仅 Z500 一个标量场（单层单变量），仅用于 nearcast 稀疏条件，不存在任何多层大气状态张量 |
| **分辨率 6km/3km** | $M_t$ 原生 3km（HRRR 混合层网格）；$S_t$ 为 25-28km 插值上采样，非真 3km 信息量 | 6km（硬件驱动的原生 3km 隔点抽样）；未证实的"V3"原生 3km 变体仅见于二手来源 | **6km 为唯一有技能评分的分辨率**；3km 仅作 domain-parallel 工程可行性演示（87.5% 并行效率），全篇无该 3km 模型的任何 FSS/CRPS 数字 |
| **预训练权重可得性** | 全文无代码/权重声明，未证实 | 有真实 MIT 协议开源仓库（NOAA-GSL/HRRRCast-live），但无确认可用的 checkpoint 下载链接，且线上仓库通道配置已与论文版本产生偏差（141 vs 180 通道） | **全文（含 Acknowledgments）无任何代码/权重声明**，grep 全文零命中；仅感谢 NVIDIA PhysicsNeMo 团队提供训练框架工具（框架本身开源≠本文模型权重开源）——与 StormCast 同一"沉默"模式 |

### 能否解决 HRRRCast 的"单时刻输入/无 96h 历史维度"根本性不匹配：**不能**

判断依据两条独立、互不依赖的理由，只要任一条成立就足以否定：

1. **数量级差距**：Stormscope 确实证明了"多帧历史堆叠输入 DiT"这一架构模式可行且在真实数据上跑赢 HRRR——这本身是对 StormCast/HRRRCast"纯单时刻马尔可夫"设计的真实架构进步。但其能承载的历史窗口（nowcast 6 帧=1 小时；nearcast 仅 1 帧）比目标 96h 小两个数量级以上，且 nearcast（更长提前量、更接近本任务 2-6h 预警场景的模式）历史窗口反而比 nowcast **更短**。这不是"部分解决、需要扩展"的量级差距，而是"验证了方向可行但规模完全不够"。
2. **表示范式更不兼容**：即使忽略历史长度问题，Stormscope 压根没有一个"4 气压层×lon×lat"的大气状态张量可供编码——它的状态空间是卫星辐射率+雷达反射率图像，只有 Z500 一个稀疏气压层标量条件。StormCast 的 $S_t$、HRRRCast 的输入张量好歹是真实的多层大气状态变量（只是层位/坐标系/信息量有各自问题），Stormscope 在"气压层覆盖"这一维度上实质上是**三者中最差的**，没有可以拆解、插值或对齐的目标对象。二者叠加，Stormscope 无法充当"缺历史维度"问题的现成解——它在解决 A 问题（历史）的同时，在 B 问题（气压层张量）上比另外两个候选更彻底地不匹配。

**唯一可迁移的价值**：如果团队最终采用已有 fallback（在自己训练折的 HRRR 96h/4 层/3km 原生窗口上做自监督预训练），Stormscope 的"历史帧沿通道维拼接 + 2D patch embedding + Neighborhood Attention 控制计算量"这一整套设计模式是一个经过真实数据验证、计算上可行的参考模板——96h 历史堆叠必然带来大量 token/通道，NA 的局部窗口注意力正是应对这类规模问题的现成、已验证方案，值得在自建模型的架构设计阶段引用为设计先例，而不是作为权重来源。

### P2 阶段 raw-only 骨干选型最终建议排序

综合两轮对 StormCast、HRRRCast、Stormscope 的全文精读，给出最终排序（从最推荐到最不推荐）：

1. **【默认推荐，不变】自建自监督预训练 fallback**——在训练折 HRRR 窗口上按目标原生形状（3km、4 气压层、96h）做轻量 masked-reconstruction/next-step 自监督预训练，再冻结接 probe head。本轮是第三个被验证"无法直接复用"的外部候选，进一步巩固而非削弱这一路径的必要性——三篇文献通读下来，没有一个在四个维度上同时满足或接近满足的现成权重来源。可借鉲 Stormscope 的"历史帧堆叠+Neighborhood Attention"模式作为自建架构的设计参考（非权重复用）。
2. **HRRRCast（2507.05658）冻结/微调部分编码器**——三者中气压层形状最接近（12 层含 2/4 精确重合）、唯一有真实开源代码仓库（虽 checkpoint 未确认）；但仍需新增一个从零训练的时序融合模块（0→96h）及 6km→3km 重新网格化，有违背"不构建新时空骨干" non-goal 的风险。
3. **StormCast（2408.10958）**——$S_t$ 分支气压层形状精确匹配目标 4 层，但信息量为粗分辨率插值、非可拆卸独立编码器；无任何权重可得性证据；与 HRRRCast 同样的 0→96h 时序缺口未解决。
4. **Stormscope（2601.17268）——本轮新增候选，排序为最末**：不具备可复用的"气压层张量编码器"这一根本对象（全文仅一个 Z500 标量条件通道），三者中气压层维度匹配度最差；虽然是唯一具备（有限）历史堆叠输入验证先例的模型，但堆叠深度（≤1h）远不足以支撑"96h 历史编码器"这一定位；同样无权重可得性证据。其价值仅限于架构设计模式参考，不构成 P2 raw-only 骨干的候选权重来源。

**给 proposal 下一轮的具体建议**：Frontier Leverage 这一 CRITICAL 缺口在连续两轮、三篇最相关文献的全文精读后，结论已经从"文献证据不足"收敛为"文献证据本身就指向自建预训练路径"——不建议再排期第三个候选模型的全文精读（如更通用的 Aurora/Prithvi-WxC 全球基座模型，其分辨率/范式差距只会比这三个更大，边际信息量预期很低）；下一轮应把预算转向自建自监督预训练 fallback 的最小可行性验证（如在既有 3 个日期的 HRRR 子集上跑一次 masked-reconstruction smoke test），而不是继续外部权重调研。

**新关键词供下一轮搜索（如仍需）**："masked reconstruction pretraining convection allowing model self-supervised"、"neighborhood attention long temporal history weather transformer"——用于验证自建预训练路径本身是否有更贴近的方法学先例可参考，而非继续搜索更多候选预训练权重源。

## Read by: 0713-scs-benchmark (2026-07-14)

**任务定位**：SCS 环境–雷达多模态开放基准数据集（ESSD / NeurIPS Datasets & Benchmarks 方向）的 deep-lit 审读。本文是观测直驱 nowcasting/nearcasting 的 2026 年前沿代表——本轮从"数据集/基准贡献"视角重读，与上一轮 0710-causal-scs 的"预训练权重候选"视角完全不同，结论方向也相反：对那条 idea 本文是"最不匹配的候选"，对本 topic 本文是"最有用的佐证与先例来源之一"。

### 撞车评估：零撞车，且是 gap 论点的旗舰级佐证

- **贡献类型不重叠**：Stormscope 是纯建模论文，不发布任何数据产品——无 curated dataset、无事件包、无 canonical splits、无代码/权重（grep 全文零开源声明，仅感谢 PhysicsNeMo 工具）。与本 topic 的数据贡献维度（填补空白/整编严谨性/可复现/可用性）没有任何 claim 级重叠。
- **"环境场非一等公民"的直接证据**：2026 年最强工业团队的观测直驱 SOTA，全文用到的大气环境信息仅 **Z500 单通道**（卫星分支）+ GFS 模拟合成反射率单通道（雷达分支），且明说是刻意设计（"To mitigate NWP error propagation and maintain operational flexibility regarding forecast latency, we intentionally limit synoptic conditioning to a single channel"）；同时 Conclusion 第一条局限自认 "denser conditioning could uncover additional skill gains"，并有消融实证（加 GFS 反射率条件全程更优、提前量越长差距越大，Supplementary Fig. ablation）。可直接引用的论证链：**SOTA 有意用极稀疏环境条件 → 作者自证更密条件有增益 → 但没有现成数据产品把长时窗环境场与雷达/事件对齐打包** —— 正是本基准要填的空白。
- **无事件标签维度**：全文与 NOAA Storm Events 或任何事件级标签零交集。其"事件中心"意识只体现在评测抽样上（nowcast 评测的 250 个 2024 冬春初始化时刻按"强降水事件发生在预报时段内"人工筛选）——连模型论文都要临时手工构造 event-centric 评测子集，本基准把这一步标准化为事件包，正中需求。

### 可复用的整编/评测先例（对 benchmark 设计直接有用）

1. **HRRR LCC 网格作为 canonical grid 的先例**：GOES ABI 原生数据重映射到 HRRR 的 3km Lambert Conformal Conic 网格（再降采 6km）；MRMS 插值到同网格同 10min 节奏；GFS/ERA5 0.25° 双线性插值到 3km 再降采 6km。本 topic 以 HRRR 为环境场源、MRMS 为雷达源——Stormscope 证明"以 HRRR LCC 网格为公共参考系谐一化多模态"是顶级团队已验证的工程选型，基准谐一化层可直接引为设计先例。
2. **年度整段 holdout split 先例**：训练 GOES 2018–2023 / MRMS 2020–2023，测试 2024 全年 360 个均匀随机初始化（与 StormCast/HRRRCast 同用 2024 评测年）。基准 canonical split 采用"按年切、整年 holdout"即与该系列工作直接可比。**注意**：MRMS 训练起点 2020 比 GOES 晚两年，暗示 MRMS 早期档案可用性/质量约束——与本 topic 风险清单中的档案质量条目一致，值得在数据描述论文中显式记录成 provenance 注记。
3. **canonical-task 评测协议全套菜单**（NeurIPS D&B 要求 tasks/splits/baselines/metrics，这里几乎现成）：FSS @ 20/30/40 dBZ × 池化 6/18/42 km 矩阵；CRPS + **滞后集合技巧**（把确定性 HRRR 变成 5 成员概率基线——解决基准"概率基线从哪来"的问题）；reliability diagram（24 成员）；object-based 验证全参数（40 dBZ、最小面积 108 km²、边界距离与质心位移 ≤40 km 匹配、POD/SR/CSI/FB，沿 WoFSCast 先例、已是社区事实标准）；配对 t 检验 + bootstrap 95% CI 显著性规范；Marshall–Palmer Z=300R^1.4 反射率→雨强转换。
4. **基线模型套餐**：pySTEPS（开源 Lucas-Kanade 光流 + 半拉格朗日外推、4 帧历史、可在雷达原生 1–2km 分辨率运行）+ HRRR + 滞后集合 HRRR。基准示范任务的 baseline 章节可直接采用同套组合。
5. **静态/派生特征先例**：太阳天顶角余弦作为计算派生通道、lat/lon 作为 dense conditioning——对应本 topic 数据模型的"静态特征"层的通道化打包方式。

### 风险/差异化注记

- **数据体量对照**：6km/10min 的 CONUS 训练已需 32×H100×48h/模型；3km 全域（1792×1024）必须 domain parallelism 且论文未给该模型任何技能分。本基准以事件聚类区域为单位裁剪（非 CONUS 全域全时段）正是绕开体量墙的差异化设计——可用 Stormscope 的算力数字反衬事件包设计对普通学术团队的可及性优势。
- **parallax 未处理**：作者承认陡视角视差误差未校正。本基准 v1（MRMS+HRRR+Storm Events）不含卫星模态、暂不触雷；未来若扩展 GOES 模态，需把 parallax 校正与否作为显式质量标记写进 provenance——这是连 Stormscope 都没做干净的点。
- **训练/推理数据源切换 = 对齐决策实例**：nearcast 训练用 ERA5 Z500（事后再分析）、推理换 GFS 预报。对建模是合理选择，对基准则是必须显式记录的时间对齐决策——本 topic"后视泄漏"风险条目的一个现实案例：事件包元数据必须区分环境场来自分析（f00/f01）还是再分析还是预报。
- **引用建议**：ESSD/D&B 论文的 Introduction（motivation：SOTA 观测直驱模型环境条件极稀疏且作者自证 headroom）与评测协议章节（FSS/object-based/滞后集合的沿用来源）都应引用本文。

### 对 landscape 的增量

Stormscope 补齐 landscape 中"观测直驱范式"的最新坐标：SEVIR 一代数据集喂养的是纯图像序列外推模型；Stormscope 这一代已经需要 NWP 条件通道但只敢给一个；下一代的数据需求（长时窗环境场 + 事件标签 + 风暴中心封装）正是本基准的供给面。反引文 4 篇 2026 新作中，2607.07879（ECMWF 纯观测全球再分析）显示 obs-direct 范式正向再分析端扩张，进一步扩大对"观测+环境场对齐打包数据产品"的需求面。

**新关键词供下一轮搜索**："event-centric severe storm dataset HRRR MRMS"、"object-based verification benchmark severe convection"（查是否已有人把 WoFSCast 式 object 验证打包成基准任务）、"lagged ensemble probabilistic baseline benchmark"、"storm events labels machine learning dataset"。
